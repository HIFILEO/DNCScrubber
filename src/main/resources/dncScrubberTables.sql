CREATE TABLE IF NOT EXISTS ColdRvmLead (
    id bigint generated by default as identity,
    dateWorkflowStarted date,
    conversationStarted boolean not null,
    toldToStop boolean not null,
    sold boolean not null,
    wrongNumber boolean not null,
    offerMade boolean not null,
    leadSentToAgent boolean not null,
    Person_id bigint,
    Property_id bigint,
    primary key (id)
 );

CREATE TABLE IF NOT EXISTS Person (
    id bigint generated by default as identity,
    firstName varchar(255),
    lastName varchar(255),
    address varchar(255),
    unitNumber varchar(255),
    city varchar(255),
    state varchar(255),
    zip varchar(255),
    county varchar(255),
    country varchar(255),
    phone1 varchar(255),
    phone1Type varchar(255),
    phone1DNC boolean not null,
    phone1Stop boolean not null,
    phone1Litigation boolean not null,
    phone1Telco varchar(255),
    phone2 varchar(255),
    phone2Type varchar(255),
    phone2DNC boolean not null,
    phone2Stop boolean not null,
    phone2Litigation boolean not null,
    phone2Telco varchar(255),
    phone3 varchar(255),
    phone3Type varchar(255),
    phone3DNC boolean not null,
    phone3Stop boolean not null,
    phone3Litigation boolean not null,
    phone3Telco varchar(255),
    email1 varchar(255),
    email2 varchar(255),
    email3 varchar(255),
    primary key (id)
 );

CREATE TABLE IF NOT EXISTS Property (
    id bigint generated by default as identity,
    address varchar(255),
    unitNumber varchar(255),
    city varchar(255),
    state varchar(255),
    zip varchar(255),
    county varchar(255),
    country varchar(255),
    APN varchar(255),
    ownerOccupied boolean not null,
    companyName varchar(255),
    companyAddress varchar(255),
    propertyType varchar(255),
    bedrooms varchar(255),
    totalBathrooms varchar(255),
    sqft integer not null,
    loftSizeSqft integer not null,
    yearBuilt integer not null,
    assessedValue integer not null,
    lastSaleRecordingDate date,
    lastSaleAmount integer not null,
    totalOpenLoans integer not null,
    estimatedRemainingBalance integer not null,
    estimatedValue integer not null,
    estimatedEquity integer not null,
    MLS_Status varchar(255),
    MLS_Date date,
    MLS_Amount varchar(255),
    lienAmount varchar(255),
    dateAddedToList date,
    person_id bigint,
    Property_id bigint,
    primary key (id)
 );
